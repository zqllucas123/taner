# 地摊小微商户线上小店小程序 - 架构设计文档

## 一、项目概述

### 1.1 产品定位

面向零基础地摊摊主的微信小程序，打通「线下摊位引流 → 线上小店成交 → 私域复购留存 → AI辅助经营」闭环。核心差异化：完全贴合地摊流动性强、客群同城、碎片化经营、摊主零基础、低成本五大特质。

### 1.2 核心用户角色

| 角色             | 描述                           | 核心诉求                                   |
| ---------------- | ------------------------------ | ------------------------------------------ |
| 摊主（Seller）   | 地摊经营者，零基础、中老年为主 | 极简操作、AI自动经营、快速出单、低门槛     |
| 顾客（Customer） | 同城消费者                     | 发现附近地摊、线上预定、优惠领取、线下自提 |

### 1.3 现有原型基础

目前 `UI/` 目录下已有一个 React + Vite 的交互原型，实现了顾客端和摊主端的核心交互流程，可作为小程序 UI 开发的参考。

---

## 二、技术选型

### 2.1 小程序框架：Taro 3.x

| 维度       | 选型理由                                                     |
| ---------- | ------------------------------------------------------------ |
| 跨端能力   | 一套代码编译为微信小程序 + H5（未来可扩展支付宝/抖音小程序） |
| React 生态 | 语法与现有原型一致，组件可直接迁移                           |
| 社区活跃   | 京东维护，生态成熟，插件丰富                                 |
| TypeScript | 原生支持，类型安全                                           |

### 2.2 前端技术栈

| 技术                | 用途      | 说明                             |
| ------------------- | --------- | -------------------------------- |
| Taro 3.x + React 18 | UI 框架   | 与现有原型技术栈对齐             |
| TypeScript          | 类型安全  | 全项目统一                       |
| NutUI / Taro UI     | UI 组件库 | 小程序原生风格组件               |
| Zustand             | 状态管理  | 轻量、无 boilerplate，适配小程序 |
| SCSS Modules        | 样式方案  | Taro 原生支持                    |

### 2.3 后端技术栈

| 技术              | 用途                                   |
| ----------------- | -------------------------------------- |
| Node.js + Express | API 服务                               |
| TypeScript        | 类型安全                               |
| MySQL 8.0         | 主数据库（订单、用户、商品）           |
| Redis             | 缓存、会话、实时状态（出摊状态热数据） |
| 腾讯云 COS        | 商品图片/短视频存储                    |
| 微信云开发        | 备选方案（轻量部署）                   |

### 2.4 AI 服务

| 能力            | 方案                              |
| --------------- | --------------------------------- |
| 语音识别（ASR） | 微信同声传译插件 / 腾讯云 ASR     |
| AI 图片优化     | 腾讯云图像处理 / 服务端调用大模型 |
| AI 定价/文案    | 服务端 DeepSeek / 通义千问 API    |
| AI 客服         | 服务端 LLM + 预设知识库 RAG       |

### 2.5 小程序插件/能力

| 插件/能力           | 用途                     |
| ------------------- | ------------------------ |
| 微信支付            | 线上支付                 |
| 微信地图/附近小程序 | 地摊点位可视化、同城曝光 |
| 微信服务通知        | 出摊提醒、订单通知       |
| 小程序订阅消息      | 顾客触达                 |
| 微信同声传译        | 语音输入                 |

---

## 三、总体架构

```
┌─────────────────────────────────────────────────────────────┐
│                       微信小程序客户端                         │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐       │
│  │  顾客端模块    │  │  摊主端模块    │  │  通用能力模块   │       │
│  │ · 地摊地图     │  │ · AI语音助手   │  │ · 微信登录     │       │
│  │ · 扫街实景     │  │ · 商品管理     │  │ · 微信支付     │       │
│  │ · 商品浏览     │  │ · 订单/预定    │  │ · 服务通知     │       │
│  │ · 预定/许愿    │  │ · 经营台账     │  │ · 地图定位     │       │
│  │ · 领券/拼团    │  │ · 大字极简模式  │  │ · 聚合码       │       │
│  │ · 打卡种草     │  │ · 歇业保护     │  │               │       │
│  └──────────────┘  └──────────────┘  └──────────────┘       │
└──────────────────────┬──────────────────────────────────────┘
                       │ HTTPS / WebSocket
┌──────────────────────▼──────────────────────────────────────┐
│                      API 网关层 (Nginx)                       │
│  · 路由分发  · 限流  · 鉴权  · 日志                          │
└──────────────────────┬──────────────────────────────────────┘
                       │
┌──────────────────────▼──────────────────────────────────────┐
│                    业务服务层 (Node.js)                       │
│  ┌─────────┐ ┌─────────┐ ┌──────────┐ ┌──────────┐        │
│  │用户服务   │ │摊位服务   │ │订单服务    │ │支付服务   │        │
│  ├─────────┤ ├─────────┤ ├──────────┤ ├──────────┤        │
│  │商品服务   │ │营销服务   │ │许愿池服务  │ │通知服务   │        │
│  ├─────────┤ ├─────────┤ ├──────────┤ ├──────────┤        │
│  │AI服务     │ │文件服务   │ │数据分析    │ │地图服务   │        │
│  └─────────┘ └─────────┘ └──────────┘ └──────────┘        │
└──────────────────────┬──────────────────────────────────────┘
                       │
┌──────────────────────▼──────────────────────────────────────┐
│                      数据层                                   │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐   │
│  │  MySQL   │  │  Redis   │  │ 腾讯云COS │  │ AI API   │   │
│  │  主库     │  │  缓存     │  │  文件存储  │  │  外部服务  │   │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘   │
└─────────────────────────────────────────────────────────────┘
```

---

## 四、小程序前端架构

### 4.1 目录结构

```
miniapp/
├── src/
│   ├── app.tsx                    # 小程序入口
│   ├── app.config.ts              # 全局配置
│   ├── app.scss                   # 全局样式
│   │
│   ├── pages/                     # 页面目录
│   │   ├── customer/              # 顾客端页面
│   │   │   ├── map/               # 地摊地图（首页）
│   │   │   │   ├── index.tsx
│   │   │   │   └── index.scss
│   │   │   ├── stall-detail/      # 摊位详情页
│   │   │   ├── product-detail/    # 商品详情页
│   │   │   ├── wish-pool/         # 许愿池
│   │   │   ├── check-in/          # 打卡种草
│   │   │   ├── coupons/           # 我的优惠券
│   │   │   └── orders/            # 我的订单
│   │   │
│   │   ├── seller/                # 摊主端页面
│   │   │   ├── dashboard/         # 经营仪表盘（首页）
│   │   │   │   ├── index.tsx
│   │   │   │   └── big-font.tsx   # 大字极简模式
│   │   │   ├── ai-assistant/      # AI语音助手
│   │   │   ├── product-manage/    # 商品管理
│   │   │   ├── pricing/           # AI定价
│   │   │   ├── order-manage/      # 订单/预定管理
│   │   │   ├── ledger/            # 经营台账
│   │   │   ├── marketing/         # 营销中心
│   │   │   ├── stall-setting/     # 摊位设置（出摊/歇业）
│   │   │   └── qrcode/            # 聚合摆摊码
│   │   │
│   │   └── common/                # 通用页面
│   │       ├── login/             # 微信授权登录
│   │       ├── payment/           # 支付页
│   │       └── settings/          # 个人设置
│   │
│   ├── components/                # 公共组件
│   │   ├── stall-card/            # 摊位卡片
│   │   ├── product-card/          # 商品卡片
│   │   ├── coupon-card/           # 优惠券卡片
│   │   ├── voice-input/           # 语音输入组件
│   │   ├── map-marker/            # 地图标记
│   │   ├── image-uploader/        # 图片上传
│   │   ├── status-badge/          # 状态标签（出摊中/已收摊等）
│   │   ├── big-button/            # 大字模式按钮
│   │   └── empty-state/           # 空状态
│   │
│   ├── stores/                    # 状态管理 (Zustand)
│   │   ├── userStore.ts           # 用户状态
│   │   ├── stallStore.ts          # 摊位状态
│   │   ├── orderStore.ts          # 订单状态
│   │   ├── cartStore.ts           # 购物车/预定车
│   │   └── uiStore.ts            # UI状态（大字模式/主题）
│   │
│   ├── services/                  # API 调用层
│   │   ├── request.ts             # 封装 wx.request
│   │   ├── authService.ts         # 认证相关
│   │   ├── stallService.ts        # 摊位相关
│   │   ├── productService.ts      # 商品相关
│   │   ├── orderService.ts        # 订单相关
│   │   ├── wishService.ts         # 许愿池相关
│   │   ├── couponService.ts       # 优惠券相关
│   │   ├── aiService.ts           # AI服务
│   │   └── paymentService.ts      # 支付相关
│   │
│   ├── hooks/                     # 自定义 Hooks
│   │   ├── useLocation.ts         # 定位
│   │   ├── useVoice.ts           # 语音
│   │   ├── useImageUpload.ts     # 图片上传
│   │   └── useStallStatus.ts     # 摊位状态实时轮询
│   │
│   ├── utils/                     # 工具函数
│   │   ├── format.ts              # 格式化
│   │   ├── storage.ts             # 本地存储
│   │   └── wx.ts                 # 微信 API 封装
│   │
│   └── constants/                 # 常量
│       ├── api.ts                 # API 路径
│       └── config.ts              # 业务配置
│
├── config/                        # Taro 编译配置
│   ├── index.ts
│   ├── dev.ts
│   └── prod.ts
│
├── types/                         # 全局类型定义
│   └── global.d.ts
│
├── package.json
├── tsconfig.json
└── project.config.json            # 微信小程序项目配置
```

### 4.2 组件树（核心页面）

```
顾客端
├── 地图首页 (MapPage)
│   ├── SearchBar（搜索栏）
│   ├── CategoryFilter（品类筛选）
│   ├── MiniMap（地图可视化）
│   │   ├── StallMarker[]（摊位标记）
│   │   └── RouteOverlay（AI逛摊路线）
│   ├── StallCard[]（摊位简略卡片）
│   └── BottomNav（底部导航）
│
├── 摊位详情 (StallDetailPage)
│   ├── VibeGallery（实景图库，支持AI优化对比）
│   ├── ProductList（商品列表）
│   │   └── ProductCard[]（含预定按钮）
│   ├── CouponSection（优惠券区）
│   ├── WishSection（许愿池区）
│   ├── GroupBuySection（拼团区）
│   └── StallInfo（摊位信息/出摊状态）
│
└── 打卡广场 (CheckInSquare)
    ├── HotTags（热门榜单）
    ├── PostList（打卡内容流）
    └── PostEditor（发布打卡）

摊主端
├── 经营仪表盘 (DashboardPage)
│   ├── StatusSwitch（出摊/歇业一键切换）
│   ├── QuickStats（营收概览卡片）
│   ├── TodayOrders（今日订单速览）
│   ├── InventoryAlert（库存预警）
│   └── AIShortcut[]（AI快捷指令）
│
├── AI语音助手 (AIAssistantPage)
│   ├── VoiceInputButton（语音输入按钮）
│   ├── ChatHistory（对话历史）
│   └── QuickCommands[]（快捷指令：上架/改价/发券/歇业）
│
├── 商品管理 (ProductManagePage)
│   ├── ProductList（商品列表，支持上下架/删改）
│   ├── ProductForm（新增/编辑表单）
│   │   ├── ImageUploader（图片上传，AI自动优化）
│   │   └── AIPriceSuggest（AI定价建议）
│   └── BatchCopy（一键复刻）
│
├── AI定价助手 (PricingPage)
│   ├── CostInput（成本/租金/同行价输入）
│   ├── PriceResult（建议价/引流价/清仓价）
│   └── ProfitCalc（利润/保本测算）
│
├── 订单管理 (OrderManagePage)
│   ├── OrderTabs（待处理/已确认/已完成）
│   ├── OrderCard[]（订单卡片）
│   └── DisputePanel（纠纷处理模板）
│
├── 经营台账 (LedgerPage)
│   ├── RevenueChart（营收图表）
│   └── LedgerList（收支明细）
│
├── 营销中心 (MarketingPage)
│   ├── CouponManager（优惠券管理）
│   ├── GroupBuyManager（拼团管理）
│   ├── FlashSale（限时秒杀）
│   └── MemberManage（会员管理）
│
└── 摊位设置 (StallSettingPage)
    ├── StallInfoForm（摊位信息编辑）
    ├── TemplateSelector（一键模板装修）
    ├── QRCodeDisplay（聚合摆摊码）
    ├── RestProtection（歇业保护设置）
    └── BigFontToggle（大字极简模式开关）
```

### 4.3 状态管理设计 (Zustand)

```typescript
// userStore - 用户身份
interface UserStore {
  user: User | null;
  role: 'customer' | 'seller';
  isLoggedIn: boolean;
  login: () => Promise<void>;
  logout: () => void;
  switchRole: () => void;
}

// stallStore - 摊位数据（核心）
interface StallStore {
  stalls: Stall[];
  currentStall: Stall | null;
  fetchStalls: (params: StallQuery) => Promise<void>;
  setCurrentStall: (id: string) => void;
  updateStatus: (status: StallStatus) => void;
}

// orderStore - 订单状态
interface OrderStore {
  orders: Order[];
  fetchOrders: (role: 'customer' | 'seller') => Promise<void>;
  createReservation: (productId: string) => Promise<void>;
  confirmOrder: (orderId: string) => void;
}

// uiStore - UI偏好
interface UIStore {
  bigFontMode: boolean;
  toggleBigFont: () => void;
  voiceEnabled: boolean;
  setVoiceEnabled: (v: boolean) => void;
}
```

---

## 五、后端服务架构

### 5.1 服务拆分

```
server/
├── src/
│   ├── index.ts                    # 入口
│   ├── config/
│   │   └── index.ts               # 环境配置
│   ├── middleware/
│   │   ├── auth.ts                # JWT 鉴权
│   │   ├── rateLimit.ts           # 限流
│   │   └── logger.ts              # 日志
│   ├── modules/
│   │   ├── user/                   # 用户模块
│   │   │   ├── user.controller.ts
│   │   │   ├── user.service.ts
│   │   │   └── user.model.ts
│   │   ├── stall/                  # 摊位模块
│   │   │   ├── stall.controller.ts
│   │   │   ├── stall.service.ts
│   │   │   └── stall.model.ts
│   │   ├── product/                # 商品模块
│   │   ├── order/                  # 订单模块
│   │   ├── wish/                   # 许愿池模块
│   │   ├── coupon/                 # 优惠券模块
│   │   ├── groupbuy/               # 拼团模块
│   │   ├── flashsale/              # 秒杀模块
│   │   ├── member/                 # 会员/积分模块
│   │   ├── checkin/                # 打卡种草模块
│   │   ├── payment/                # 支付模块
│   │   ├── withdrawal/             # 提现模块
│   │   ├── ledger/                 # 台账模块
│   │   ├── ai/                     # AI模块
│   │   │   ├── ai.controller.ts
│   │   │   ├── ai.service.ts
│   │   │   ├── pricing.ts         # AI定价
│   │   │   ├── imageOptimize.ts   # AI图片优化
│   │   │   ├── copywriting.ts     # AI文案生成
│   │   │   └── customerService.ts # AI客服
│   │   ├── notification/          # 通知模块
│   │   └── upload/                # 文件上传模块
│   ├── common/
│   │   ├── db.ts                  # MySQL 连接
│   │   ├── redis.ts              # Redis 连接
│   │   ├── cos.ts                # COS 上传
│   │   └── wechat.ts             # 微信 API 封装
│   └── types/
│       └── index.ts
├── package.json
└── tsconfig.json
```

### 5.2 核心 API 设计

```
# 用户认证
POST   /api/auth/login                  # 微信登录（wx.login code 换 JWT）
GET    /api/auth/profile                # 获取用户信息
PUT    /api/auth/profile                # 更新用户信息

# 摊位
GET    /api/stalls                      # 摊位列表（支持按距离/品类/状态筛选）
GET    /api/stalls/:id                  # 摊位详情
POST   /api/stalls                      # 创建摊位（30秒极速开店）
PUT    /api/stalls/:id                  # 更新摊位信息
PUT    /api/stalls/:id/status           # 切换出摊/歇业状态
GET    /api/stalls/:id/qrcode           # 获取聚合摆摊码

# 商品
GET    /api/stalls/:id/products         # 摊位商品列表
POST   /api/stalls/:id/products         # 新增商品（支持AI图片优化）
PUT    /api/products/:id               # 更新商品
DELETE /api/products/:id               # 下架商品
POST   /api/products/:id/copy          # 一键复刻商品
POST   /api/products/:id/ai-pricing    # AI定价建议

# 订单/预定
POST   /api/orders/reserve             # 创建预定订单
GET    /api/orders                      # 订单列表（按角色）
PUT    /api/orders/:id/confirm          # 确认订单（摊主）
PUT    /api/orders/:id/cancel           # 取消订单
POST   /api/orders/:id/dispute          # 发起纠纷仲裁

# 许愿池
GET    /api/stalls/:id/wishes           # 许愿列表
POST   /api/stalls/:id/wishes           # 提交许愿
PUT    /api/wishes/:id                  # 更新许愿状态（摊主回复）
GET    /api/wishes/hot                  # 全站许愿热度

# 优惠券
GET    /api/stalls/:id/coupons          # 优惠券列表
POST   /api/stalls/:id/coupons          # 创建优惠券
POST   /api/coupons/:id/claim           # 领取优惠券

# 拼团
GET    /api/stalls/:id/groupbuys        # 拼团列表
POST   /api/stalls/:id/groupbuys        # 发起拼团
POST   /api/groupbuys/:id/join          # 加入拼团

# 秒杀
GET    /api/stalls/:id/flash-sales      # 秒杀活动列表
POST   /api/stalls/:id/flash-sales      # 创建秒杀（限时一元福利）
POST   /api/flash-sales/:id/redeem      # 秒杀核销（线下）

# 会员与积分
GET    /api/member/profile              # 会员档案（等级/积分）
GET    /api/member/points               # 积分流水
POST   /api/member/points/exchange      # 积分兑换优惠券

# 打卡种草
GET    /api/checkins                     # 打卡广场内容流
POST   /api/checkins                     # 发布打卡（奖励积分）
POST   /api/checkins/:id/like            # 点赞打卡

# 聚合码
GET    /api/stalls/:id/qrcode           # 获取聚合摆摊码
POST   /api/qrcode/scan                  # 扫码进店（统计 + 路由功能）

# 支付
POST   /api/payment/wechat-pay          # 微信支付下单
POST   /api/payment/callback            # 支付回调

# 提现
GET    /api/withdrawals                  # 提现记录
POST   /api/withdrawals                  # 发起提现（当日/定期/自动）

# 经营台账
GET    /api/ledger/summary              # 营收概览
GET    /api/ledger/records              # 收支明细

# AI服务
POST   /api/ai/voice-command            # 语音指令处理
POST   /api/ai/pricing                  # AI定价计算
POST   /api/ai/image-optimize           # AI图片优化
POST   /api/ai/copywriting              # AI文案生成
POST   /api/ai/customer-service         # AI客服问答

# 通知
POST   /api/notification/send           # 发送服务通知

# 上传
POST   /api/upload/image                # 图片上传
POST   /api/upload/video                # 短视频上传
```

---

## 六、数据模型设计

> 完整的表结构（DDL）、字段说明、索引设计、Redis 键设计与分阶段建表清单，已独立维护于 **[数据库设计文档](./地摊小微商户线上小店小程序-数据库设计文档.md)**。本节仅保留领域划分与核心实体关系概览。

### 6.1 领域划分

数据库按 7 个业务域组织，与服务层模块一一对应：

| 领域 | 核心表 |
| ---- | ------ |
| 用户与会员 | `users` `member_profiles` `point_transactions` |
| 摊位 | `stalls` `stall_schedules` `stall_media` `stall_qrcodes` `stall_certs` `markets` `market_stalls` |
| 商品 | `products` `pricing_records` `inventory_logs` |
| 交易 | `orders` `order_items` `payments` `withdrawals` `ledgers` |
| 营销 | `coupons` `user_coupons` `group_buys` `group_buy_participants` `flash_sales` `referrals` |
| 互动 | `wishes` `checkins` `checkin_likes` `notifications` |
| AI | `ai_voice_logs` `ai_conversations` |

### 6.2 核心实体关系

```
User (用户/摊主同账号体系)
  ├── 1:1 ─── MemberProfile (会员档案：等级/积分)
  ├── 1:N ─── Stall (摊位)        # 一个摊主可有多个摊位
  ├── 1:N ─── Order (订单)        # 用户可下多个订单
  ├── 1:N ─── Wish (许愿)
  ├── 1:N ─── CheckIn (打卡种草)
  └── 1:N ─── PointTransaction (积分流水)

Stall (摊位)
  ├── 1:N ─── Product (商品) ──── 1:N PricingRecord / InventoryLog
  ├── 1:N ─── Coupon (优惠券) ─── 1:N UserCoupon (领取记录)
  ├── 1:N ─── GroupBuy (拼团) ─── 1:N GroupBuyParticipant
  ├── 1:N ─── FlashSale (秒杀)
  ├── 1:N ─── StallSchedule (出摊时段)
  ├── 1:N ─── StallMedia (实景照片/视频/全景)
  ├── 1:N ─── StallCert (资质/食品安全证)
  ├── 1:1 ─── StallQRCode (聚合摆摊码)
  └── N:1 ─── Market (市集商圈)

Order (订单)
  ├── N:1 ─── User / Stall
  ├── 1:N ─── OrderItem (订单项)
  └── 1:N ─── Payment (支付记录)
```

### 6.3 设计要点

- **账号体系统一**：摊主与顾客共用 `users`，通过 `role` 切换，`has_stall` 标记是否开店。
- **嵌套数据拆表**：原型 `Stall` 中内联的 products/coupons/wishes/groupBuys 在数据库中均拆为独立表，通过 `stall_id` 关联。
- **金额与并发**：金额统一 `DECIMAL`；库存、优惠券、拼团、秒杀等高并发扣减走 Redis 原子操作，异步落库。
- **地理位置**：原型的 `coordinate{x,y}` 模拟坐标改为真实经纬度，支持同城距离查询。

---

## 七、核心业务流程

### 7.1 顾客预定下单流程

```
顾客浏览地图/商品 → 选择商品 → 填写取货时间/备注
  → 选择优惠券（可选） → 提交预定
  → 生成取货码 → 推送摊主新订单通知
  → 摊主确认备货 → 顾客到摊位凭码取货 → 完成
```

### 7.2 摊主商品上架流程

```
摊主拍照/选择图片 → AI自动去背景优化
  → 语音/文字输入商品名、进价
  → AI定价建议（参考同行/成本/点位）
  → 一键确认上架 → 实时同步到顾客端
```

### 7.3 歇业保护流程

```
触发方式：手动点击 / 语音指令 / 定时自动
  → 摊位状态 → offline
  → 线上商品 → 暂停预定
  → 已有订单 → 自动发通知告知改期
  → 地图标记 → 灰色打烊中
```

---

## 八、非功能性设计

### 8.1 性能

- 小程序首屏加载 < 2s（分包加载）
- 地图标记懒加载，可视区域渲染
- 商品图片 CDN + WebP 格式
- Redis 缓存热门摊位数据（TTL 5分钟）

### 8.2 安全

- JWT Token + 微信 session_key 双重验证
- API 接口防重放攻击
- 支付回调签名验证
- 用户隐私数据加密存储

### 8.3 可维护性

- 组件粒度：页面 → 区块模块 → 基础组件
- 服务端模块化拆分，单一职责
- 统一错误码规范
- 接口文档自动生成（Swagger）

### 8.4 可扩展性

- 摊位品类/模板可配置扩展
- AI 服务接口抽象，可切换不同模型
- 支付渠道可扩展（先微信支付，后续支付宝等）

---

## 九、分阶段开发计划

### 一期（MVP）：核心闭环 + 差异化功能

| 模块         | 功能点                                |
| ------------ | ------------------------------------- |
| 微信登录     | 一键授权注册                          |
| 摊位管理     | 30秒极速开店、出摊/歇业切换、点位标注 |
| 商品管理     | 图片上传、定价、上下架、AI定价建议    |
| 预定系统     | 顾客线上下单 → 摊主确认 → 线下自提  |
| AI语音助手   | 语音指令：上架/改价/发券/歇业         |
| 摆摊聚合码   | 扫码进店+加微信+领券+预定             |
| 大字极简模式 | 中老年摊主适配                        |
| 许愿池       | 顾客许愿 + 摊主回复                   |
| 地图可视化   | 摊位经纬度标注、距离排序、出摊状态    |
| 优惠券       | 创建/领取/核销                        |

### 二期：AI深度赋能 + 社交裂变

| 模块         | 功能点                          |
| ------------ | ------------------------------- |
| AI图文短视频 | 自动去背景/调色、朋友圈文案生成 |
| AI经营台账   | 自动记账、营收报表              |
| 双向许愿池   | AI汇总全站许愿热度指导进货      |
| 邻里拼团     | 2-3人成团，摊位自提             |
| 电子会员     | 积分累计/抵扣/等级自动升级      |
| 实景预览     | 摊位360°轻量化实景             |
| 用户打卡种草 | 逛摊打卡发布图文                |

### 三期：生态扩展

| 模块           | 功能点                 |
| -------------- | ---------------------- |
| 市集商圈抱团   | 多摊位聚合商圈         |
| AI客源研判     | 顾客偏好分析、补货建议 |
| 收款自动发福利 | 线下支付自动发券       |
| 合伙分账       | 多摊主分账             |
| AI逛玩路线规划 | 智能规划夜市路线       |

---

## 十、部署架构

```
┌──────────────────────────────────────────┐
│           微信小程序（Taro 编译）           │
│         部署到微信小程序平台               │
└────────────────┬─────────────────────────┘
                 │
┌────────────────▼─────────────────────────┐
│          Nginx（API 网关）                 │
│          腾讯云 CVM / 轻量服务器           │
└────────────────┬─────────────────────────┘
                 │
     ┌───────────┼───────────┐
     ▼           ▼           ▼
┌─────────┐ ┌─────────┐ ┌─────────┐
│Node.js  │ │Node.js  │ │Node.js  │
│实例 1    │ │实例 2    │ │实例 3    │
│(PM2)    │ │(PM2)    │ │(PM2)    │
└────┬────┘ └────┬────┘ └────┬────┘
     │           │           │
     └───────────┼───────────┘
                 ▼
     ┌─────────────────────┐
     │    MySQL 8.0 主从   │
     │    Redis 缓存集群   │
     └─────────────────────┘
```

### 推荐方案（轻量起步）

- **服务器**：腾讯云轻量应用服务器 2C4G（约 100元/月）
- **数据库**：腾讯云 MySQL 基础版（约 50元/月）
- **Redis**：腾讯云 Redis 256MB（约 30元/月）
- **存储**：腾讯云 COS 按量计费
- **AI 服务**：DeepSeek API / 通义千问 API（按 token 计费，初期可免费额度）

> 备选方案：使用微信云开发（CloudBase），无需自建服务器，适合极低成本起步，但灵活性略低。

---

## 十一、与现有原型的关系

`UI/` 目录下的 React 原型是交互设计验证版本，用于快速验证用户体验和业务流程。小程序正式开发时：

1. **复用逻辑**：types.ts 的类型定义、mockData 的数据结构可直接参考（字段到数据库表的映射见[数据库设计文档](./地摊小微商户线上小店小程序-数据库设计文档.md)第十二节）
2. **复用交互**：CustomerApp / SellerApp 的页面布局、交互流程作为 UI 参照
3. **需要改造**：React DOM 组件 → Taro 小程序组件，CSS → SCSS Modules，状态管理 → Zustand
4. **需要新增**：真实 API 对接、微信原生能力集成（支付/地图/通知）

---

## 十二、相关文档

| 文档 | 说明 |
| ---- | ---- |
| [数据库设计文档](./地摊小微商户线上小店小程序-数据库设计文档.md) | 完整表结构、索引、Redis 键、分阶段建表清单 |
| [功能规划（PRD）](./PRD/地摊小微商户线上小店小程序%20全套功能规划（原有+新增完整版）.md) | 全套功能需求 |
| [竞品分析](./竞品分析/地摊夜市同类产品竞品调研（2026最新）+%20项目差异化优势.md) | 竞品调研与差异化 |

---

> 文档版本: v1.1
> 最后更新: 2026-06-24
> 变更：数据库设计独立拆分至《数据库设计文档》；补充会员/积分、打卡、秒杀、提现、聚合码扫码等 API 与服务模块
